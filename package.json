{
  "name": "makarfi",
  "homepage": "https://rexali.github.io/maikarfi",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "predeploy":"rm -rf build && mkdir build && rsync -r -av --progress . build/ --exclude node_modules --exclude ProfileMaikarfi2024.pdf --exclude .git --exclude build",
    "deployed": "gh-pages -d ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }   
}
